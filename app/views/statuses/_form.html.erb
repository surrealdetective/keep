<%= form_for [@idea, @status] do |f| %>
  <% if @status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, :class => "form control" %>
  </div><br>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content, :class => "form-control" %>
  </div></br>
  <div class="actions">
    <%= f.submit :class => "btn btn-md btn-primary" %>
  </div>
<% end %>